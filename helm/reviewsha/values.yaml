namespace: reviewsha
imagePullSecrets: []

images:
  api: { repository: reviewsha-api, tag: dev, pullPolicy: IfNotPresent }
  worker: { repository: reviewsha-worker, tag: dev, pullPolicy: IfNotPresent }
  web: { repository: reviewsha-web, tag: dev, pullPolicy: IfNotPresent }
  admin: { repository: reviewsha-admin, tag: dev, pullPolicy: IfNotPresent }

replicas:
  api: 2
  worker: 2
  web: 2
  admin: 2

# Pipeline stages can be processed by different worker replicas. Keep their
# job workspace on a shared volume so download/extract/parse/merge stages see
# the same files when BullMQ schedules them on different pods.
worker:
  persistence:
    enabled: true
    size: 10Gi
    accessModes: [ReadWriteOnce]
    storageClass: ''
    existingClaim: ''

omniroute:
  enabled: true
  image: { repository: ghcr.io/diegosouzapw/omniroute, tag: latest, pullPolicy: IfNotPresent }
  replicas: 2
  port: 20128
  existingSecret: reviewsha-omniroute-secrets
  seedConfigMap: ''
  persistence:
    enabled: true
    size: 1Gi
    storageClass: ''
  resources:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: '1', memory: 2Gi }

resources:
  api:
    requests: { cpu: 100m, memory: 256Mi }
    limits: { cpu: 500m, memory: 512Mi }
  worker:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: '1', memory: 1Gi }
  web:
    requests: { cpu: 50m, memory: 64Mi }
    limits: { cpu: 250m, memory: 256Mi }
  admin:
    requests: { cpu: 50m, memory: 64Mi }
    limits: { cpu: 250m, memory: 256Mi }

env:
  NODE_ENV: production
  API_PORT: '3000'
  API_PREFIX: api/v1
  CORS_ORIGIN: http://app.reviewsha.test:18080,http://admin.reviewsha.test:18080
  REDIS_URL: redis://redis:6379
  REDIS_HOST: redis
  REDIS_PORT: '6379'
  MINIO_ENDPOINT: ''
  OMNIROUTE_DASHBOARD_URL: http://localhost:20128
  # Reasoning-capable models need room for both hidden reasoning and the
  # structured JSON review response.
  AI_MAX_TOKENS: '2500'
  AI_INPUT_MAX_TOKENS: '2500'
  AI_MODEL: ds-web/deepseek-chat
  # Respect upstream 429 lockouts instead of hammering the provider from both
  # worker replicas. The provider can override the delay with Retry-After.
  AI_RETRY_ATTEMPTS: '5'
  AI_RETRY_DELAY_MS: '5000'
  AI_RETRY_MAX_DELAY_MS: '120000'
  AI_MAX_CONCURRENCY: '1'

secretName: reviewsha-secrets
secrets:
  create: false

services:
  api: { port: 3000 }
  web: { port: 80 }
  admin: { port: 80 }

ingress:
  enabled: false
  className: nginx
  host: reviewsha.local
  tls: false

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 2
  targetCPUUtilizationPercentage: 70
