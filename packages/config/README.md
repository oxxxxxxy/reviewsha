# @reviewsha/config

Shared configuration package for Reviewsha.

Contains:

- API constants;
- queue names;
- bucket names;
- JWT constants;
- pagination defaults;
- upload limits;
- environment keys;
- default local URLs;
- shared Zod env validation helpers.

Public API is exported only through `src/index.ts`.
