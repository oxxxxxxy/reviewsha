-- Reviewsha local development database bootstrap.
-- Business schema and Prisma migrations are applied by the API workspace in later stages.
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
